interface ISelectedItem {
  userId?: number | any;
  date?: Date | any;
  menuId?: number | any;
  createdAt?: Date | any;
  updatedAt?: Date | any;
}

interface User {
  id: number;
}

interface Menu {
  id: number;
  // other fields for the Menu model
}
